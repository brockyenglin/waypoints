# Archived: SVG story plates
Flat SVG map figures from an earlier page design (mule deer / woodcock / pigeon).
Kept for reuse as static social-export templates. generate-story-maps.mjs needs
d3-geo + us-atlas + world-atlas (still in package.json) and reads src/data/*.json.
